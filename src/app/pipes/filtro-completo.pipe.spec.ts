import { FiltroCompletoPipe } from './filtro-completo.pipe';

describe('FiltroCompletoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCompletoPipe();
    expect(pipe).toBeTruthy();
  });
});
